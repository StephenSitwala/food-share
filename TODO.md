# Task: Fix hamburger/close icon appearing on desktop view after clicking navbar links

## Steps
- [x] Fix the navbar link click handler in `scripts/app.js` so it only resets the mobile icons when the mobile menu is active (prevents hamburger/close icons from showing on desktop).
- [ ] Test on desktop view (>=1024px) that the hamburger and close icons remain hidden after clicking any navbar link.
